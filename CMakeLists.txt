# Establecer la versión mínima de CMake requerida
cmake_minimum_required(VERSION 3.10)

# Nombre del proyecto
project(MyProject)

# Configurar la compilación del ejecutable
add_executable(mi_ejecutable direccion_de_los_archivos)